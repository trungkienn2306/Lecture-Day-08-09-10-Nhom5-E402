# === Python ===
.env
.env.local
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# === macOS / Windows ===
.DS_Store
Thumbs.db
desktop.ini

# === IDE ===
.vscode/
.idea/
*.suo
*.user

# === ChromaDB (local vector store — regenerable, không commit) ===
chroma_db/

# === Test / CI cache ===
.pytest_cache/
.coverage
htmlcov/
.hypothesis/

# === Pipeline logs (sinh ra khi chạy, không cần commit) ===
artifacts/logs/
*.log

# === HuggingFace / sentence-transformers model cache ===
.cache/

# === Misc ===
*.tmp
*.bak

# Artifacts được commit (bằng chứng nộp bài):
#   artifacts/manifests/   → manifest JSON mỗi run
#   artifacts/quarantine/  → quarantine CSV mỗi run
#   artifacts/cleaned/     → cleaned CSV mỗi run
#   artifacts/eval/        → before_after_eval.csv, grading_run.jsonl
